Tool {{tool_name}} not found.

Use capability discovery before retrying guessed tool names:
- call `capabilities:list` for the full runtime-discovered inventory
- call `capabilities:describe` for verbose details on one specific tool/skill

Current available tools/skills context:
\n{{tools_prompt}}